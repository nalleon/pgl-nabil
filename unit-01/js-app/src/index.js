import React from 'react';
import ReactDOM from 'react-dom';
import ComponenteApp from './ComponenteApp.js';

const divRoot = document.getElementById("root");
ReactDOM.render(
  //<React.StrictMode>
    <ComponenteApp />, divRoot
  //</React.StrictMode>
);
